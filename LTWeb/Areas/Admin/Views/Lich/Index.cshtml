@model PagedList.IPagedList<Model.EF.CapDau>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Quản lý người dùng";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}
@section header{
    Danh sách cặp đấu
}
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-body">
                @using (Html.BeginForm("Index", "Lich", FormMethod.Get))
                {
                    <div class="row form-group">
                        <div class="col-lg-12">
                            <div class="col-md-3">
                                <a class="btn btn-primary" href="~/Admin/Lich/Create">Thêm mới</a>
                            </div>
                        </div>
                    </div>
                }
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Đội nhà</th>
                                <th>Dội Khách</th>
                                <th>Giờ Đấu</th>
                                <th>Ngày đấu</th>
                                <th>meta</th>
                                <th>Trạng thái</th>
                                <th>#</th>
                            </tr>
                        </thead>
                        <tbody>

                            @foreach (var item in Model)
                            {
                                <tr id=" row_@item.IDcapDau">
                                    <td>@item.IDcapDau</td>
                                    <td>@item.DoiNha</td>
                                    <td>@item.DoiKhach</td>
                                    <td>@item.GioDau</td>
                                    <td>@item.NgayDau</td>
                                    <td>@item.meta</td>
                                    <td><a class="btn-active" data-id="@item.IDcapDau">@Html.Raw(item.hide ? "<strong>Kích hoạt</strong>" : "Khoá")</a></td>
                                    <td>
                                        <a href="~/Admin/Lich/Edit/@item.IDcapDau">Sửa</a>
                                        @Ajax.ActionLink("Xóa", "Delete", new { @id = item.IDcapDau }, new AjaxOptions
                                   {
                                       Confirm = "Bạn có chắc xóa cặp đấu này không ?",
                                       OnComplete = "$('#row_" + @item.IDcapDau + "').Remove()",
                                       HttpMethod = "Delete"
                                   })
                                    </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                    @Html.PagedListPager(Model, page => Url.Action("Index", new { page })) @*//Phân trang Table*@
                </div>
            </div>
        </div>
    </div>
</div>